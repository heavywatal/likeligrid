---
title: "Exclusivity model"
author: "WMI"
date: "December 24, 2016"
output:
  html_document:
    css: "~/.R/rmd.css"
    self_contained: true
    theme: null
    highlight: null
    mathjax: https://cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-AMS_CHTML
---

- pathway: $i$
    - weight: $w_i$, where $\sum_i w_i = 1$
    - exclusivity:  $x_i$
    - number of mutated sites: $s_i$
- configuration: $j$
    - number of mutated sites: $s_j = \sum_i s_i$
    - unnormalized probability of the configuration:
      $p_j = \prod _i w_i^{s_i} x_i^{\max(s_i - 1, 0)}$
    - normalized probability of the configuration:
      $P_j = p_j / D_{s_j}$
    - denominator to normalize probability:
      $D_s = \sum_j^{s = s_j} p_j$.


```{r setup, include=FALSE, eval=FALSE}
library(knitr)
opts_chunk$set(out.width='100%')
library(pipeR)
library(tidyverse)
library(wtl)
loadNamespace('cowplot')
ggplot2::theme_set(wtl::theme_wtl())
```
